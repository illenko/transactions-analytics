export enum Direction {
    Expenses = 'expenses',
    Income = 'income'
}

export enum Unit {
    Month = 'month',
    Day = 'day'
}

export enum Calculation {
    Cumulative = 'cumulative',
    Absolute = 'absolute'
}

export enum Value {
    Amount = 'amount',
    Count = 'count'
}